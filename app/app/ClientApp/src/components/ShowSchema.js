import React, { Component } from "react";

export class ShowSchema extends Component {
    static displayName = ShowSchema.name;

    render() {
        return (
            <div>
                <h1>Affichage d'un schema</h1>
            </div>
        );
    }
}